// This file is intentionally public. It must contain only the API address, never keys.
window.__AISCU_CONFIG__ = {
  // This makes the published site use the backend on this Windows computer.
  // Replace with an HTTPS API domain only after the cloud migration is complete.
  apiBaseUrl: "http://127.0.0.1:3001",
};
